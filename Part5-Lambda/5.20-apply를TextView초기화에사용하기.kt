package `Part5-Lambda`

//fun createViewWithCustomAttributes(context: Context) =
//    TextView(context).apply { // 새로운 textView 인스턴스를 만들고 즉시 그 인스턴스를 apply에 넘긴다.
//        text = "Sample Text"
//        textSize = 20.0
//        setPadding(10,0,0,0)
//    } // apply는 람다에 의해 초기화 된 textView 인스턴스를 반환한다.
